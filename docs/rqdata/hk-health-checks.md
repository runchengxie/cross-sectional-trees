# HK 数据健康检查

HK 数据健康检查、current contract 审计、asset audit、PIT coverage、intraday health 和 release workflow 已从 `cross-sectional-trees` sunset。

当前职责边界：

* `market-data-platform`：下载、检查、清洗、PIT、current contract、dataset registry、release。
* `cross-sectional-trees`：读取数据平台产物，执行研究、回测、持仓快照和执行目标导出。

保留本页是为了旧链接不断裂。新的维护命令和报告格式以 `market-data-platform` 文档为准。
