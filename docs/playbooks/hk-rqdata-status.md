# HK RQData 状态

本页过去记录 `cross-sectional-trees` 内部 HK RQData 资产状态。该职责已经 sunset。

当前权威状态在 `market-data-platform` 中维护；本仓库不再记录 HK asset snapshot、alias、quota、health report 或 release 状态。研究侧只消费已经生成的数据文件和标准层。

保留本页是为了旧文档链接不断裂。新流程请从 `docs/playbooks/hk-data-assets.md` 和 `docs/concepts/data-sources.md` 进入。
