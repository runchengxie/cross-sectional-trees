## Monthly HK selected baseline with total-return price proxy and the
## calibrated balanced execution profile. This is the most direct local
## variant when you want better research realism without adding heavier data.
extends:
  - configs/experiments/variants/hk_selected__execution_balanced_local.yml

data:
  price_col: tr_close
  cache_tag: hk_selected_tr_close_exec_balanced_local_v1

eval:
  run_name: hk_sel_xgb_reg_tr_close_exec_balanced_local
