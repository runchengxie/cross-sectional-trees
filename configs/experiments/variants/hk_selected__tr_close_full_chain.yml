## Monthly HK selected baseline with a unified total-return price chain.
## Price-based features, labels, backtest, and benchmark all follow tr_close.
extends:
  - configs/experiments/baseline/hk_selected.yml

data:
  price_col: tr_close
  cache_tag: hk_selected_tr_close
  rqdata:
    adjust_type: pre

eval:
  run_name: hk_sel_xgb_reg_tr_close
