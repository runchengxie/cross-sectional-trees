## Same quarterly PIT + overlay data route, but replace the ranker with
## xgb_regressor to test whether OOS drift is model-family specific.
extends:
  - hk_selected__quarterly_pit_core_hybrid_provider_overlay_validate_dense_wf_base.yml
  - configs/presets/models/xgb_regressor.yml

model:
  params:
    learning_rate: 0.03

eval:
  run_name: hk_sel_q_g4_fixed_pit_overlay_xgb_reg_validate
